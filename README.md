api: xử lý http request api

components: chứa các UI component

constanst: Chưa các biến const dùng chung

hook:custom hook

serive: tạo realtime socket, các service api

store: api call và lưu trữ (thao tác với zustands)

utils: chứa các helpers